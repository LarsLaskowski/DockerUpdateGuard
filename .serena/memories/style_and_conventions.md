C# uses file-scoped namespaces, one top-level type per file, extensive #region blocks, XML docs on public/internal/private members, Allman braces, var preferred, no primary constructors, no this. prefix, and == false style for boolean negation. Runtime/test projects target net10.0, link SharedAssemblyInfo.cs, use nullable + implicit usings, Reihitsu analyzer, and MSTest naming like {Feature}Tests / {Class}{Scenario}{ExpectedResult}. Use apply_patch for edits and avoid unrelated changes.